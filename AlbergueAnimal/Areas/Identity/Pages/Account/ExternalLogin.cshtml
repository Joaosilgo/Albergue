@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Registar";
}

<h2>@ViewData["Title"]</h2>
<h4>Associar a sua conta @Model.LoginProvider.</h4>
<hr />

<p class="text-info">
    Autenticou-se com sucesso com <strong>@Model.LoginProvider</strong>.
    Por favor insira um email de Autenticação para este site.
    Carregue no butão Registo para para realizar logging in.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Registar</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

